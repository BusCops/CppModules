#include "Replace.hpp"

int main(int ac, char **av)
{
	if (ac == 4)
	{
		std:: file = av[1];
		std:: 1 = av[2];
		std:: 2 = av[3];
		if (file.empty() | 1.empty())
		{
			std::cout << "file or  1 cant be empty" << std::endl;
			return 0;
		}
		Replace rep(1, 2, file);
		rep.ReplaceStrings();
	}
	else
		std::cout << "programe input are :\n-> <filename>\n-> 1\n-> 2" << std::endl;
}

